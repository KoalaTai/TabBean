const config = {
  backendUrl: 'http://localhost:3000', // Change this for production
  openAiApiKey: 'your-openai-api-key', // Replace with actual API key
  elasticsearchNode: 'http://localhost:9200', // Change this if Elasticsearch is hosted elsewhere
  // Add other configuration options as needed
};

export default config;
